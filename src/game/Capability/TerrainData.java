package game.Capability;

/**
 * Can be use when we need to find out information about a terrain object from outside of its class
 */
public enum TerrainData {
    TREE,
    BUSH,
    ADJTREE,
    SIPPED,
    LAKE,
    WALL;
}
